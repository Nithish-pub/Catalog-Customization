name: store_catalog
description: Store-level product catalog application

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  dio: ^5.0.0
  json_annotation: ^4.8.1
  shared_preferences: ^2.2.0
  flutter_cache_manager: ^3.3.1
  hive: ^2.2.3
  firebase_performance: ^0.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - .env 